<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="notice">
	<typeAlias alias="noticeListVO" type="com.jungam.manage.vo.NoticeListVO"/>
	
	
	<select id="list" resultClass="noticeListVO">
	  SELECT INDEX		AS index
	  		,TITLE		AS title
			,WRITER		AS writer
			,CONTENT	AS content
			,HITCOUNT	AS hitCount
	  FROM NOTICES
	</select>
	
	<select id="getnotice" parameterClass="int" resultClass="noticeListVO">
	  SELECT INDEX		AS index
	  		,TITLE		AS title
			,WRITER		AS writer
			,CONTENT	AS content
			,HITCOUNT	AS hitCount
	  FROM NOTICES
	  WHERE INDEX=#no#
	</select>
	
	<select id="savenotice" parameterClass="noticeListVO">
		INSERT INTO 
		NOTICES(TITLE, WRITER, CONTENT)
		VALUES(#title#, #writer#, #content#)
	</select>
</sqlMap>